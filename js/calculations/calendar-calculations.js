export function getGameDateKey(game={}){const value=game.date??game.gameDate??"";if(value?.toDate)value=value.toDate();if(value instanceof Date){const parts=new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Tokyo",year:"numeric",month:"2-digit",day:"2-digit"}).formatToParts(value);const part=type=>parts.find(item=>item.type===type)?.value;return `${part("year")}-${part("month")}-${part("day")}`}const text=String(value).trim(),match=text.match(/(\d{4})\D+(\d{1,2})\D+(\d{1,2})/);return match?`${match[1]}-${match[2].padStart(2,"0")}-${match[3].padStart(2,"0")}`:""}
export function getDatesWithRegisteredGames(games=[],yearMonth=""){return new Set(games.map(getGameDateKey).filter(key=>key&&(!yearMonth||key.startsWith(`${yearMonth}-`))))}
export function calendarMonthFor(selected="",fallback=new Date()){const key=String(selected).match(/^(\d{4})-(\d{2})/);if(key)return `${key[1]}-${key[2]}`;const parts=new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Tokyo",year:"numeric",month:"2-digit"}).formatToParts(fallback);return `${parts.find(p=>p.type==="year").value}-${parts.find(p=>p.type==="month").value}`}
export function moveCalendarMonth(yearMonth,delta){const [year,month]=yearMonth.split("-").map(Number),date=new Date(Date.UTC(year,month-1+delta,1));return `${date.getUTCFullYear()}-${String(date.getUTCMonth()+1).padStart(2,"0")}`}
export function buildCalendarDays(yearMonth){const [year,month]=yearMonth.split("-").map(Number),first=new Date(Date.UTC(year,month-1,1)).getUTCDay(),days=new Date(Date.UTC(year,month,0)).getUTCDate();return [...Array(first).fill(null),...Array.from({length:days},(_,i)=>`${yearMonth}-${String(i+1).padStart(2,"0")}`)]}
