# R32 Basketball Stats Ver.3.2 patch

## 変更内容
- 試合一覧から開く試合詳細の個人スタッツは、4Q換算せず入力値をそのまま表示。
- 分析タブの「試合別」は、4Q換算せず入力値をそのまま表示。
- ホーム、大会別、日別、月別、年別は従来通り4Q換算平均。
- REB内訳のOR/DR枠幅を統一するCSSを追加。

## アップロード方法
このZIPを解凍し、`index.html` をGitHubリポジトリ直下の `index.html` に上書きしてください。
必要に応じて他のファイルは既存のままで構いません。
