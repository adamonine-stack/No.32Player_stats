import assert from "node:assert/strict";import { readFileSync } from "node:fs";
const app=readFileSync(new URL("../js/app.js",import.meta.url),"utf8"),index=readFileSync(new URL("../index.html",import.meta.url),"utf8");for(const marker of ["collection(db,'seasons')","scopedCollection('playerSeasons',seasonId)","where('seasonId','==',seasonId)","seasonId:gameSeasonId","seasonId:effectiveSeasonId(g,state.selectedSeasonId)","migration前バックアップ","seasonMigrationVersion:1","この世代から外す"])assert.ok(app.includes(marker),marker);assert.ok(index.includes("styles/seasons.css"));console.log("season app contract: ok");
